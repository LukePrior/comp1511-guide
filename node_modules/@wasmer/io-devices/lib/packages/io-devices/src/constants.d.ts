export declare const IO_DEVICES_CONSTANTS: {
    FILE_PATH: {
        DEVICE_FRAMEBUFFER_ZERO: {
            PATH: string;
            FRAME_BUFFER: string;
            VIRTUAL_SIZE: string;
            DRAW: string;
            INPUT: string;
        };
    };
};
